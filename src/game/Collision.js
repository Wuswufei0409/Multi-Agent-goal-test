/**
 * Collision — 碰撞检测与响应（AABB / 圆形）。
 * 阶段 0：建立模块骨架；检测逻辑在阶段 3 实现。
 */
export class Collision {
  constructor() {
    // 阶段 0 占位。
  }

  /**
   * aabbOverlap(a, b) — 阶段 3 实现。
   */
  aabbOverlap(_a, _b) {
    void _a;
    void _b;
    return false;
  }
}
