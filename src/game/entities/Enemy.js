/**
 * Enemy — 敌机实体。
 * 阶段 0：建立实体骨架；生成/波次/行为在阶段 3 实现。
 */
export class Enemy {
  constructor() {
    this.x = 0;
    this.y = 0;
    this.width = 40;
    this.height = 40;
  }

  update(_dt) {
    // 阶段 3 实现。
    void _dt;
  }
}
