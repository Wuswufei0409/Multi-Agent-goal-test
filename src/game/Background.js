/**
 * Background — 滚动星空/云层背景。
 * 阶段 0：建立模块骨架；滚动绘制在阶段 1 实现。
 */
export class Background {
  /**
   * @param {HTMLCanvasElement} _canvas
   */
  constructor(_canvas) {
    // 阶段 0 占位。
    void _canvas;
  }

  update(_dt) {
    // 阶段 1 实现星空滚动。
    void _dt;
  }

  draw(_ctx, _width, _height) {
    // 阶段 1 实现。
    void _ctx;
    void _width;
    void _height;
  }
}
